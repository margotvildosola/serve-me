import React from 'react';

const Input = () => (
    <div>
        <label htmlFor="serverConfigName">Name</label>
        <input type="text" id="serverConfigName" />
    </div>
);

export default Input;
